import styled from 'styled-components';

export const Outer = styled.div`
  > label {
    padding: 5px 10px;
    text-align: center;
    display: block;
  }

  > label:hover {
    background-color: var(--almost-black);
  }
`;
